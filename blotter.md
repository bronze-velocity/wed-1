2026-07-13 — Explained Turbopack panic: pnpm node_modules symlinks were created on Windows and contain relative paths that escape the Linux filesystem root when the Windows drive is mounted under `/run/media/...`. Fix is `rm -rf node_modules && pnpm install` from Linux.
2026-07-17 16:09 — Created zz/info/repo-structure.md: brief structured overview of the entire repo (routes, components, data, styling, public assets, reference docs, config files)
2026-07-17 — Advised user to install pnpm via `npm install -g pnpm` since pnpm was missing from the Linux environment.
2026-07-17 — Updated .gitignore: removed dead TypeScript block (project is JS-only) and unused Yarn/PnP rules; added Windows OS junk (Thumbs.db, ehthumbs.db, Desktop.ini), editor dirs (.vscode/, .idea/), general *.log, and *.local override pattern.
2026-07-17 — Fixed pnpm `ERR_PNPM_IGNORED_BUILDS` for sharp: replaced placeholder `allowBuilds`/`ignoredBuiltDependencies` in pnpm-workspace.yaml with `onlyBuiltDependencies: [sharp, @tailwindcss/oxide, unrs-resolver]`. pnpm v11 no longer reads the `pnpm` field in package.json.
2026-07-17 16:36 — Fixed constant "Fast Refresh rebuilding" / UI glitching in `pnpm run dev`. Root cause: Turbopack in Next 16.2.9 panicked with "Next.js package not found" on every HMR check (pnpm nested-peer layout `.pnpm/next@16.2.9_react-dom@…_react@…__react@…/` trips its Rust resolver); panic log had the same trace 286 times. Fix: switched `dev` script to `next dev --webpack` in package.json. Alternatives noted: downgrade Next 15.x, or `node-linker=hoisted` in .npmrc.
